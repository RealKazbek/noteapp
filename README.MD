# NoteApp (Task Manager)

Проект для управления задачами, состоящий из бэкенда на Django и фронтенда на React/TypeScript.

## Структура проекта

- **django/** — Серверная часть (Backend API)
- **TaskManagerApp/** — Клиентская часть (Frontend)

---

## 🚀 Как запустить проект

### Предварительные требования

Убедитесь, что у вас установлены:

- [Python](https://www.python.org/) (версии 3.8 или выше)
- [Node.js](https://nodejs.org/) (для запуска фронтенда)
- Git

### 1. Настройка и запуск Бэкенда (Django)

Откройте терминал и выполните следующие шаги:

1.  Перейдите в папку с бэкендом:

    ```bash
    cd django
    ```

    _(Если файл `manage.py` находится глубже, например в `django/taskmanager`, перейдите туда)_.

2.  Создайте и активируйте виртуальное окружение (рекомендуется):

    - **Windows:**
      ```powershell
      python -m venv venv
      .\venv\Scripts\activate
      ```
    - **macOS/Linux:**
      ```bash
      python3 -m venv venv
      source venv/bin/activate
      ```

3.  Установите зависимости:

    ```bash
    pip install -r requirements.txt
    ```

    _(Если файла `requirements.txt` нет, установите Django вручную: `pip install django djangorestframework django-cors-headers`)_.

4.  Примените миграции базы данных:

    ```bash
    python manage.py migrate
    ```

5.  Запустите сервер:
    ```bash
    python manage.py runserver
    ```
    Бэкенд будет доступен по адресу: `http://127.0.0.1:8000/`

---

### 2. Настройка и запуск Фронтенда (TaskManagerApp)

Откройте **новый** терминал (не закрывая терминал с Django) и выполните:

1.  Перейдите в папку приложения:

    ```bash
    cd TaskManagerApp
    ```

2.  Установите зависимости (node_modules):

    ```bash
    npm install
    ```

3.  Запустите приложение:
    ```bash
    npm start
    ```
    Обычно приложение открывается автоматически по адресу: `http://localhost:3000/`

---

## 🛠 Основные команды

- **Создать суперпользователя (админка):**
  Перейдите в папку с `manage.py` и введите:

  ```bash
  python manage.py createsuperuser
  ```

- **Обновить список зависимостей (Python):**
  ```bash
  pip freeze > requirements.txt
  ```

## Автор

[RealKazbek](https://github.com/RealKazbek)
